"use client";
import { useEffect, useState } from "react";
import { AlertTriangle, BarChart3, RefreshCw } from "lucide-react";
import { PageHeader } from "@/components/ui";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { cn } from "@/lib/utils";
const stages = [{ key: "impressions", label: "Impressões" }, { key: "clicks", label: "Cliques" }, { key: "lpv", label: "Page views" }, { key: "leads", label: "Leads" }, { key: "checkout", label: "Checkout" }, { key: "purchases", label: "Compras" }];
export default function FunilPage() { const [clients, setClients] = useState<any[]>([]); const [period, setPeriod] = useState("7"); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); async function load() { setLoading(true); try { const r = await fetch(`/api/funnel?period=${period}`, { cache: "no-store" }); const d = await r.json(); if (!r.ok) throw new Error(d.error); setClients(d.clients || []); } catch (e: any) { setError(e?.message || "Falha ao carregar funil."); } finally { setLoading(false); } } useEffect(() => { load(); }, [period]); if (loading && !clients.length) return <div className="p-4 md:p-6 md:ml-56 space-y-4"><Skeleton className="h-8 w-48" /><Skeleton className="h-48 rounded-lg" /></div>; return <div className="p-4 md:p-6 md:ml-56 pb-20 md:pb-6 space-y-4 animate-fade-in"><PageHeader title="Funil 360" subtitle="Onde o resultado está vazando por cliente." actions={<div className="flex items-center gap-2"><select value={period} onChange={(e) => setPeriod(e.target.value)} className="h-9 rounded-lg border border-border bg-transparent px-3 text-sm"><option value="7">Últimos 7 dias</option><option value="14">Últimos 14 dias</option><option value="30">Últimos 30 dias</option></select><Button variant="secondary" size="sm" onClick={load} disabled={loading}><RefreshCw className={cn("mr-1 h-3.5 w-3.5", loading && "animate-spin")} />{loading ? "Atualizando…" : "Atualizar"}</Button></div>} />{error && <div className="rounded border border-red-500/20 bg-red-500/10 px-3 py-2 text-sm text-red-500"><AlertTriangle className="mr-1 inline h-4 w-4" />{error}</div>}<div className="space-y-3">{clients.map((client) => { const max = Math.max(...stages.map((stage) => Number(client[stage.key] || 0)), 1); const first = Number(client.impressions || 0); const values = stages.map((stage) => Number(client[stage.key] || 0)); const minIndex = values.findIndex((value, index) => index > 0 && values[index - 1] > 0 && value / values[index - 1] < 0.2); return <section key={client.client_id} className="rounded-xl border border-border/50 bg-card p-4"><div className="mb-3 flex items-center gap-2"><BarChart3 className="h-4 w-4 text-primary" /><h2 className="text-sm font-semibold">{client.name}</h2>{minIndex >= 0 && <span className="rounded-full bg-amber-500/10 px-2 py-0.5 text-[10px] font-semibold text-amber-600">Gargalo: {stages[minIndex].label}</span>}</div><div className="grid grid-cols-2 gap-2 md:grid-cols-6">{stages.map((stage, index) => <div key={stage.key} className="rounded-lg border border-border/40 p-3"><div className="text-[11px] text-muted-foreground">{stage.label}</div><div className="mt-1 text-lg font-bold">{Number(client[stage.key] || 0).toLocaleString("pt-BR")}</div><div className="mt-2 h-1.5 rounded-full bg-muted"><div className="h-full rounded-full bg-primary" style={{ width: `${Math.max(4, Number(client[stage.key] || 0) / max * 100)}%` }} /></div>{index > 0 && Number(client[stages[index - 1].key] || 0) > 0 && <div className="mt-1 text-[10px] text-muted-foreground">{(Number(client[stage.key] || 0) / Number(client[stages[index - 1].key] || 1) * 100).toFixed(1)}% da etapa anterior</div>}</div>)}</div></section>; })}{!clients.length && <div className="rounded border border-dashed border-border p-10 text-center text-sm text-muted-foreground">Sem dados de funil no período.</div>}</div></div>; }
